export enum Tag {
  Player = 'player',
  Enemy = 'enemy',
  Spawn = 'spawn',
  RoomTrigger = 'roomTrigger',
  Key = 'key',
  Block = 'block',
  World = 'world',
}

export enum ZIndex {
  World = 0,
  Objects = 1,
}
